spring:
  application:
    name: user-service

  config:
    import: optional:configserver:http://config-service:8888

  messages:
    basename: messages

  # ADD MAIL CONFIG HERE
  mail:
    host: smtp.gmail.com
    port: 587
    username: devhicham00@gmail.com
    password: xvrqfdgwqejuwuwm
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
        debug: true

# ADD APP CONFIG HERE
app:
  activation:
    token:
      expiry-hours: 24
  email:
    from: noreply@booking-container.com
    activation-subject: "Activate Your Booking Container Account"
  frontend:
    url: https://192.168.151.122:7000
    
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://discovery-service:8761/eureka/

server:
  port: 8082
  address: 0.0.0.0

logging:
  level:
    org.springframework.cloud.config: DEBUG
    org.springframework.security: DEBUG
    org.springframework.security.web.access.ExceptionTranslationFilter: DEBUG
    org.springframework.security.web.FilterChainProxy: DEBUG
    org.springframework.security.web.authentication: DEBUG
    org.springframework.security.web.access.intercept.FilterSecurityInterceptor: DEBUG
    org.springframework.security.access.intercept.AbstractSecurityInterceptor: DEBUG