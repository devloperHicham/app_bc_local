spring:
  application:
    name: discovery-service

  cloud:
    config:
      uri: http://config-service:8888

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always